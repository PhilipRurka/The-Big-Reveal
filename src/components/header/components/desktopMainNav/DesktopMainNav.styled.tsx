import styled from 'styled-components';

export const DesktopMainNavWrapper = styled.div`

`;

export const PageList = styled.ul`
  display: flex;
`;

export const PageItem = styled.li`
  margin-right: 20px;
  
  &:last-child {
    margin: 0;
  }
`;